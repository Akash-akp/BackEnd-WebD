{
  "name": "basicserver",
  "version": "1.0.0",
  "description": "second server",
  "main": "index.js",
  "scripts": {
    "install": "npm install node && npm install ejs&&npm install express&&npm install mongoose&&npm install express-ejs-layouts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
